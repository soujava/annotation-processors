package org.soujava.metadata.compiler;

import java.util.function.Function;

public class EntityMetadataFactory implements Function<Class<?>, EntityMetadata> {

    @Override
    public EntityMetadata apply(Class<?> type) {

        return null;
    }
}
